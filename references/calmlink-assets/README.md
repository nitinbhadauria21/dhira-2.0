# CalmLink DHIRA illustration assets

Designer originals from `assets-used/` (see ASSETS.md).

Copy into `public/illustrations/` and reference as `/illustrations/<file>`.

Buddy cut-outs are transparent PNGs with edge-flood-fill background removal —
preserve that method if re-exporting.
